import utilStyles from "./../styles/utils.module.css";

const Custom500: React.FC = () => {
	return <h1 className={utilStyles.headingXl}>500 - Server-side error ocurred</h1>;
};

export default Custom500;
